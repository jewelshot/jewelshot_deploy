/**
 * Translations Index
 */

import { en } from './en';
import { tr } from './tr';
import { Translations } from '../types';

export const translations: Translations = {
  en,
  tr,
};

export { en, tr };
